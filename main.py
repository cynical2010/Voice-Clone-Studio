"""
Voice Clone Studio - Main entry point

This is a transitional main file. The full refactor into modules is in progress.
For now, this imports and runs the monolithic voice_clone_studio.py
"""

if __name__ == "__main__":
    # Import and run the existing monolithic app
    import voice_clone_studio
    print("Voice Clone Studio is launching...")
    print("Note: Module refactoring in progress. Currently using legacy monolithic structure.")
